<?php

	// do we want to encrypt (base64) urls and strings?
	define( 'ENCODE_64', 0 );

	// what is the path (if any) to our application.
	define( 'ALIAS_URL', '/maps' );

	// want to filter page to add href="?page=" args
	define( 'FILTER_A',  0 );
